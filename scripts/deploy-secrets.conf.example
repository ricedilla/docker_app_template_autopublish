# GitHub Secrets Configuration File
# Copy this file to deploy-secrets.conf and fill in your values
# DO NOT commit deploy-secrets.conf to git - add it to .gitignore!

# Server hostname or IP address
DEPLOY_HOST=subdomain.roca.tools

# SSH username (typically 'rice' for shared deployment)
DEPLOY_USER=rice

# Option 1: Specify path to SSH private key file
SSH_PRIVATE_KEY_FILE=/path/to/your/private/key

# Option 2: OR paste the SSH private key content directly (uncomment the lines below)
# SSH_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
# MIIEpAIBAAKCAQEA1234567890abcdef...
# (paste your full private key content here)
# ...
# -----END RSA PRIVATE KEY-----"

# Note: Use either SSH_PRIVATE_KEY_FILE or SSH_PRIVATE_KEY, not both
# The script will use SSH_PRIVATE_KEY_FILE if both are specified
